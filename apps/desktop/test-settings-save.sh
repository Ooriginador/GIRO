#!/usr/bin/env bash
# ═══════════════════════════════════════════════════════════════════════════
# Script de Teste: Validar Salvamento de Configurações
# ═══════════════════════════════════════════════════════════════════════════

set -e

DB_PATH="/home/jhonslife/Mercearias/apps/desktop/src-tauri/mercearias.db"

echo "🧪 Teste de Salvamento de Configurações"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# ────────────────────────────────────────────────────────────────────────────
# 1. Verificar Schema
# ────────────────────────────────────────────────────────────────────────────

echo "1️⃣  Verificando schema da tabela settings..."
SCHEMA=$(sqlite3 "$DB_PATH" "PRAGMA table_info(settings);" | cut -d'|' -f2)
EXPECTED_COLS="id key value type group_name description updated_by_id created_at updated_at"

echo "   Colunas encontradas:"
for col in $SCHEMA; do
    echo "   ✓ $col"
done

# Validar colunas essenciais
for col in id key value type group_name created_at updated_at; do
    if ! echo "$SCHEMA" | grep -q "$col"; then
        echo "   ❌ Coluna '$col' não encontrada!"
        exit 1
    fi
done

echo "   ✅ Schema correto!"
echo ""

# ────────────────────────────────────────────────────────────────────────────
# 2. Testar INSERT manual
# ────────────────────────────────────────────────────────────────────────────

echo "2️⃣  Testando INSERT manual..."
TEST_ID=$(uuidgen | tr '[:upper:]' '[:lower:]')
TEST_KEY="test.setting.$(date +%s)"
TEST_VALUE="test_value_123"

sqlite3 "$DB_PATH" <<SQL
INSERT INTO settings (id, key, value, type, group_name, created_at, updated_at)
VALUES (
    '$TEST_ID',
    '$TEST_KEY',
    '$TEST_VALUE',
    'STRING',
    'test',
    datetime('now'),
    datetime('now')
);
SQL

echo "   ✅ INSERT executado com sucesso!"
echo ""

# ────────────────────────────────────────────────────────────────────────────
# 3. Verificar dados inseridos
# ────────────────────────────────────────────────────────────────────────────

echo "3️⃣  Verificando dados inseridos..."
RESULT=$(sqlite3 "$DB_PATH" "SELECT key, value, type, group_name FROM settings WHERE key = '$TEST_KEY';")

if [ -z "$RESULT" ]; then
    echo "   ❌ Dados não encontrados!"
    exit 1
fi

echo "   Resultado: $RESULT"
echo "   ✅ Dados encontrados!"
echo ""

# ────────────────────────────────────────────────────────────────────────────
# 4. Testar UPDATE
# ────────────────────────────────────────────────────────────────────────────

echo "4️⃣  Testando UPDATE..."
NEW_VALUE="updated_value_456"

sqlite3 "$DB_PATH" <<SQL
UPDATE settings 
SET value = '$NEW_VALUE', updated_at = datetime('now')
WHERE key = '$TEST_KEY';
SQL

UPDATED=$(sqlite3 "$DB_PATH" "SELECT value FROM settings WHERE key = '$TEST_KEY';")

if [ "$UPDATED" != "$NEW_VALUE" ]; then
    echo "   ❌ UPDATE falhou! Esperado: $NEW_VALUE, Recebido: $UPDATED"
    exit 1
fi

echo "   ✅ UPDATE executado com sucesso!"
echo ""

# ────────────────────────────────────────────────────────────────────────────
# 5. Limpar dados de teste
# ────────────────────────────────────────────────────────────────────────────

echo "5️⃣  Limpando dados de teste..."
sqlite3 "$DB_PATH" "DELETE FROM settings WHERE key = '$TEST_KEY';"
echo "   ✅ Cleanup concluído!"
echo ""

# ────────────────────────────────────────────────────────────────────────────
# 6. Verificar configurações existentes
# ────────────────────────────────────────────────────────────────────────────

echo "6️⃣  Configurações existentes no banco:"
echo ""
sqlite3 -header -column "$DB_PATH" "SELECT key, value, type, group_name FROM settings ORDER BY group_name, key;"
echo ""

# ────────────────────────────────────────────────────────────────────────────
# Resultado Final
# ────────────────────────────────────────────────────────────────────────────

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ TODOS OS TESTES PASSARAM!"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "⚠️  Próximo passo: Testar no frontend (interface)"
